import { TypeConversionPipe } from './type-conversion.pipe';

describe('TypeConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
